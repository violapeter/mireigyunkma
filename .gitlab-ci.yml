stages:
  - deploy
image: node:latest
before_script:
  - npm ci
pages:
  stage: deploy
  script:
    - npm run build
    - cp ./dist/* ./public
  artifacts:
    paths:
      - public
  only:
    - master
